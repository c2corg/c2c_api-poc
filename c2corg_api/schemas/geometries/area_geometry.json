{
  "type": "object",
  "properties": {
    "geom_detail": {
      "properties": {
        "type": { "enum": ["Polygon"] },
        "coordinates": {
          "$comment": "an array of array of float (if linestring)."
        }
      },
      "required": ["type", "coordinates"],
      "additionalProperties": false
    }
  },
  "required": ["geom_detail"],
  "additionalProperties": false
}
